{
  "builds": [
    {
      "src": "app.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "translations/**"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/app.ts" }
  ]
}
